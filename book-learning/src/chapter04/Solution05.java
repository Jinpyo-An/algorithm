/*
1. arr1의 행의 길이가 행의 길이고, arr2의 열의 길이가 열의 길이인 배열을 선언
2. arr1와 arr2를 곱한 값을 result 배열에 넣는다.
   2-1.
3. result를 반환한다.
 */

package chapter04;

public class Solution05 {

    public static int[][] solution(int[][] arr1, int[][] arr2) {
        int row = arr1.length;
        int column = arr2[0].length;

        int[][] result = new int[row][column];

        for (int i = 0; i < row; i++) {
            for (int j = 0; j < column ; j++) {

            }
        }
    }
}
